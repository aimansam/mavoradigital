services:
  web:
    build: .
    ports:
      - "4321:4321"
    env_file:
      - path: .env
        required: false
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm dev --host 0.0.0.0
